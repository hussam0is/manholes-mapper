# Database ERD — Manholes Mapper

Entity-relationship diagram of the tables **actually used** by the app (Neon Postgres).
Source of truth: `api/_lib/db.js` (`initializeDatabase()`) + `scripts/migrate-auth-tables.js` (Better Auth).
Unused tables and fields have been removed from this diagram — see [Removed / excluded items](#removed--excluded-items) at the bottom.

Solid lines = real foreign-key constraints. Dashed lines = logical joins (no FK constraint in the DB).

## Application tables

```mermaid
erDiagram
    organizations ||--o{ projects : "organization_id CASCADE"
    organizations ||--o{ users : "organization_id SET NULL"
    projects ||--o{ sketches : "project_id SET NULL"
    projects ||--o{ project_layers : "project_id CASCADE"
    users ||..o{ sketches : "user_id"
    sketches ||..o{ issue_comments : "sketch_id"
    users ||..o{ issue_comments : "user_id"
    issue_comments ||--o{ issue_notifications : "comment_id CASCADE"
    sketches ||..o{ issue_notifications : "sketch_id"
    users ||..o{ issue_notifications : "user_id"
    users ||..o{ user_features : "target_id when target_type is user"
    organizations ||..o{ user_features : "target_id when target_type is organization"

    organizations {
        uuid id PK
        text name
        timestamptz created_at
    }

    users {
        text id PK "same value as Better Auth user.id"
        text username
        text email UK
        text role "user | admin | super_admin"
        uuid organization_id FK "nullable"
        timestamptz created_at
        timestamptz updated_at
    }

    projects {
        uuid id PK
        uuid organization_id FK
        text name
        text description "nullable"
        jsonb input_flow_config "conditional field rules"
        numeric target_km "completion forecasting - nullable"
        timestamptz created_at
        timestamptz updated_at
    }

    sketches {
        uuid id PK
        text user_id "owner - Better Auth user id"
        uuid project_id FK "nullable"
        text name
        timestamptz creation_date "date shown in exports"
        jsonb nodes "node objects incl createdAt createdBy"
        jsonb edges "edge objects incl createdAt createdBy"
        jsonb admin_config "per-sketch admin settings"
        jsonb snapshot_input_flow_config "frozen copy of project config"
        text created_by "username"
        text last_edited_by "username"
        text locked_by "edit-lock holder user id"
        timestamptz locked_at
        timestamptz lock_expires_at "30-min lock expiry"
        integer version "optimistic-locking counter"
        timestamptz created_at
        timestamptz updated_at "auto via trigger"
    }

    project_layers {
        uuid id PK
        uuid project_id FK
        text name
        text layer_type "sections | survey_manholes | survey_pipes | streets | addresses"
        jsonb geojson "FeatureCollection in ITM coords"
        jsonb style "color lineWidth opacity fill labelField"
        boolean visible
        integer display_order
        timestamptz created_at
        timestamptz updated_at
    }

    user_features {
        uuid id PK
        text target_type "user | organization"
        text target_id "user id or org id - polymorphic"
        text feature_key "export_csv export_sketch admin_settings finish_workday node_types edge_types"
        boolean enabled
        timestamptz created_at
    }

    issue_comments {
        uuid id PK
        uuid sketch_id "no FK constraint"
        text node_id "Issue node id inside sketch JSONB"
        text user_id "commenter"
        text username
        text content
        boolean is_close_action
        boolean is_reopen_action
        timestamptz created_at
    }

    issue_notifications {
        uuid id PK
        text user_id "recipient"
        uuid sketch_id "no FK constraint"
        text node_id
        uuid comment_id FK "nullable"
        text type "new_comment | issue_closed | issue_reopened | mention"
        boolean read
        timestamptz created_at
    }
```

## Better Auth tables

Managed by the Better Auth library (email/password only, no OAuth, no email verification).
`users.id` (app table above) stores the same value as `user.id`.

```mermaid
erDiagram
    user ||--o{ session : "userId CASCADE"
    user ||--o{ account : "userId CASCADE"

    user {
        text id PK "uuid generated by app"
        text name
        text email UK
        boolean emailVerified "always false - required by library"
        timestamptz createdAt
        timestamptz updatedAt
    }

    session {
        text id PK
        text token UK "7-day expiry"
        text userId FK
        timestamptz expiresAt
        timestamptz createdAt
        timestamptz updatedAt
    }

    account {
        text id PK
        text accountId
        text providerId "always credential"
        text userId FK
        text password "hashed"
        timestamptz createdAt
        timestamptz updatedAt
    }
```

## Removed / excluded items

Tables and fields that exist in the schema/scripts but are **not used** by the app, and are therefore left out of the diagrams above:

| Item | Why it's unused | Can it be dropped? |
|------|-----------------|--------------------|
| `rate_limits` table | Only created/used by `checkDbRateLimit()` in `api/_lib/rate-limit.js`, which is never called by any route (all routes use the in-memory limiter). Also flagged in `docs/SECURITY-AUDIT-2026-04-20.md`. | Yes — safe to drop, plus the dead `checkDbRateLimit()` code. |
| `verification` table | Better Auth email-verification/reset storage; `requireEmailVerification: false` and no reset flow, so it stays empty. | No — Better Auth requires the table to exist. Keep, but it holds no data. |
| `account.accessToken`, `refreshToken`, `idToken`, `accessTokenExpiresAt`, `refreshTokenExpiresAt`, `scope` | OAuth-only fields; the app uses email/password (`providerId = 'credential'`) exclusively. | Risky — Better Auth's schema expects them; dropping may break inserts. Keep in DB, ignore. |
| `user.image` | No avatar feature; never read anywhere in the frontend. | Same as above — part of Better Auth's core schema. |
| `session.ipAddress`, `session.userAgent` | Written by Better Auth on sign-in but never read by the app. | Same as above. |
| `sketch_locks` table | **Does not exist.** Mentioned in `CLAUDE.md` docs, but locking is implemented as `locked_by` / `locked_at` / `lock_expires_at` columns on `sketches`. | Documentation fix only. |

### Note on stale `api/_lib/schema.sql`

`api/_lib/schema.sql` has drifted from the runtime schema in `db.js`: it declares `sketches.user_id` and `users.id` as `UUID` (runtime uses `TEXT`), and is missing the lock columns, `version`, `projects.target_km`, `issue_comments`, and `issue_notifications`. The runtime `initializeDatabase()` in `api/_lib/db.js` is authoritative.
